import { type Metadata } from "next";
export const metadata: Metadata = {
    title: "管理控制台",
    description: "管理控制台",
};


const AdminConsole = () => {
    return (
        "好的"
    );
};

export default AdminConsole;